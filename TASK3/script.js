// Get the elements by their class or tag names
const leftNav = document.querySelector(".left");
const searchInput = document.querySelector(".search");
const profile = document.querySelector(".profile");
const wishlist = document.querySelector(".wishlist");
const bag = document.querySelector(".bag");

// Add event listeners or perform any actions on the elements
leftNav.addEventListener("click", function(event) {
  // Handle click event on left navigation menu
});

searchInput.addEventListener("input", function(event) {
  const searchValue = event.target.value;
  // Perform search operation based on the input value
});

profile.addEventListener("click", function(event) {
  // Handle click event on profile
});

wishlist.addEventListener("click", function(event) {
  // Handle click event on wishlist
});

bag.addEventListener("click", function(event) {
  // Handle click event on bag
});
